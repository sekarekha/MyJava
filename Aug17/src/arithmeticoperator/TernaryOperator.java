package arithmeticoperator;

public class TernaryOperator {
    public static void main(String args[]) {
        double number = -5.5;
        String result = (number > 0.0) ? "Positive" : "Negative";
        System.out.println(number + " is " + result);
    }
}
